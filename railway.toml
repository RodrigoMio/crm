# Build: use nixpacks.toml (fases install/build com provider "node").
# Não use buildCommand aqui — no Nixpacks atual pode gerar RUN sem npm no PATH.

[deploy]
#startCommand = "node scripts/railway-start.cjs"
startCommand = "node backend/dist/main.js"
